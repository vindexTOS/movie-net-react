import React from 'react'

const NavBar = () => {
  const style = {
    nav: `w-[100%] h-[100px] bg-white bg-opacity-10 absolute`,
  }
  return <nav className={style.nav}></nav>
}

export default NavBar
